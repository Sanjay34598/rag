# Leapcell Deployment Configuration
service:
  name: voice-rag-backend
  type: web
  runtime: python3.11
  root_directory: backend
  build_command: pip install -r requirements.txt
  start_command: uvicorn app.main:app --host 0.0.0.0 --port 8080 --workers 1
  port: 8080
